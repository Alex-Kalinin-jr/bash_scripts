promtool #command that comes with Prometheus to syntax check your configuration and rules.

/etc/prometheus/prometheus.yml #here is default config file

#for reload 
killall -HUP prometheus
sudo systemctl daemon-reload	#  (when the --web.enable-lifecycle flag is enabled).
sudo systemctl restart prometheus

#for reload according to docs
kill -s SIGTERM <PID> # to shutdown prometheus
kill -s SIGHUP <PID> #to reload config

